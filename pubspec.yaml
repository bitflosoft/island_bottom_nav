name: island_bottom_nav
description: iOS island-style floating bottom navigation bar for Flutter, with a composable app shell and right-aligned island FAB.
version: 0.1.0
repository: https://github.com/bitflosoft/island_bottom_nav

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  very_good_analysis: ^10.2.0
